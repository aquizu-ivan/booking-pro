{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install --frozen-lockfile && pnpm --filter @booking-pro/api build"
  },
  "deploy": {
    "startCommand": "pnpm --filter @booking-pro/api start"
  }
}
